import { TarrefaConcluidaDirective } from './tarefa-concluida.directive';

describe('TarrefaConcluidaDirective', () => {
  it('should create an instance', () => {
    const directive = new TarrefaConcluidaDirective();
    expect(directive).toBeTruthy();
  });
});
